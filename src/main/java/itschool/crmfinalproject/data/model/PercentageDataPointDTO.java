package itschool.crmfinalproject.data.model;

/**
 * Represents a data point that indicates a percentage value for a specific category.
 */
public record PercentageDataPointDTO(String category, Double percentage) {
}
